# --- /while : refine iteratively until done ---

/routine use first
/set iteration = 1
/while iteration <= 3
    /print "the iteration is $iteration"
    /sleep 3s
    /set iteration = iteration + 1
/end
# lets exit
/exit
